// Generated by CoffeeScript 1.3.3
(function() {

  app.implement(function(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
  }, ['utils', 'string', 'capitalise'], false);

  app.implement(function(string) {
    return string.replace(/^\s\s*/, '').replace(/\s\s*$/, '');
  }, ['utils', 'string', 'trimSpaces'], false);

  app.implement(function(string, limit, decorator) {
    if (string.length > limit) {
      return string.substr(0, string.length - decorator.length) + decorator;
    }
    return string;
  }, ['utils', 'string', 'truncate'], false);

}).call(this);
