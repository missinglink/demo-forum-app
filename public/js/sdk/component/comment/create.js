// Generated by CoffeeScript 1.3.3
(function() {

  app.implement(function() {
    var conf, hideForm, onAddComment, self, showForm;
    self = {};
    conf = app.config.get('component.comment');
    onAddComment = function(data) {
      return $('[data-role="comments-stack"][data-post-id="' + data['post-id'] + '"]').prepend(app.template.render('components.comment', data));
    };
    showForm = function(e) {
      var postId;
      postId = $(this).attr('data-post-id');
      $(conf.selectors.commentForm).filter("[data-post-id='" + postId + "']").show();
      return e.preventDefault();
    };
    hideForm = function(e) {
      var postId;
      postId = $(this).attr('data-post-id');
      $(conf.selectors.commentForm).filter("[data-post-id='" + postId + "']").hide();
      return e.preventDefault();
    };
    self.onAdd = onAddComment;
    self.showForm = showForm;
    self.hideForm = hideForm;
    return self;
  }, ['component', 'comment'], true);

}).call(this);
