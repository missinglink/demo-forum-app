// Generated by CoffeeScript 1.3.3
(function() {

  $(function() {
    var bindFormActions, conf, execFormAction;
    conf = app.config.get('form');
    execFormAction = function(form) {
      var action;
      action = app.form.action.get(form);
      if (typeof action === 'function') {
        app.form.action.perform(form);
        return true;
      }
      return false;
    };
    bindFormActions = function(form) {
      return app.event.bind('submit', 'form', function(evt) {
        var validates;
        form = $(this);
        if (form.is(conf.selectors.validationForm)) {
          validates = app.form.validate(form);
          if (validates === false) {
            return false;
          }
        }
        return !execFormAction(form);
      });
    };
    return bindFormActions();
  });

}).call(this);
