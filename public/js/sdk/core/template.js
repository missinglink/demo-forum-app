// Generated by CoffeeScript 1.3.3
(function() {

  app.implement(function() {
    var getTemplate, getTemplates, renderString, renderTemplate, self, setTemplate, setTemplates, templates;
    self = {};
    templates = {};
    setTemplate = function(ns, template) {
      return app.declare(template, templates, ns.replace(/\//g, '.').split('.'), false);
    };
    setTemplates = function(templates) {
      var template, _i, _len, _results;
      _results = [];
      for (_i = 0, _len = templates.length; _i < _len; _i++) {
        template = templates[_i];
        _results.push(setTemplate(template.ns, template.markup));
      }
      return _results;
    };
    getTemplate = function(nsString) {
      return app.utils.namespace.fetch(templates, nsString.split('.'));
    };
    renderTemplate = function(templateNs, data) {
      var template;
      template = getTemplate(templateNs);
      if (template instanceof Hogan.Template === false) {
        template = Hogan.compile(template);
      }
      return template.render(data);
    };
    renderString = function(str, data) {
      return Hogan.compile(str).render(data);
    };
    getTemplates = function() {
      return templates;
    };
    self.set = setTemplate;
    self.get = getTemplate;
    self.render = renderTemplate;
    self.renderString = renderString;
    self.getAll = getTemplates;
    return self;
  }, ['template'], true);

}).call(this);
