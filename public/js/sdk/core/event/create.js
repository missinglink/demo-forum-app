// Generated by CoffeeScript 1.3.3
(function() {

  app.implement(function() {
    var actions, bindEvent, body, catchEvent, conf, getBody, init, self, triggerEvent;
    self = {};
    actions = {};
    conf = app.config.get('event');
    body = null;
    getBody = function() {
      return body = $('body');
    };
    catchEvent = function(eventName, func) {
      return body.on(conf.prefix + eventName, func);
    };
    triggerEvent = function(eventName, data) {
      return body.trigger(conf.prefix + eventName, data);
    };
    bindEvent = function(evtType, selector, func) {
      return $(function() {
        return body.on(evtType, selector, func);
      });
    };
    init = function() {
      return getBody();
    };
    $(function() {
      return init();
    });
    self.trigger = triggerEvent;
    self.on = catchEvent;
    self.bind = bindEvent;
    return self;
  }, ['event'], true);

}).call(this);
