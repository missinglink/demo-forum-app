// Generated by CoffeeScript 1.3.3
(function() {

  app.implement(function(wrapper, conf, onChange) {
    var bindLabelClick, getCurrent, init, labels, onLabelClick, openTab, self, tabs;
    self = {};
    labels = wrapper.find(conf.selectors.labels);
    tabs = wrapper.find(conf.selectors.tabs);
    openTab = function(tabId) {
      labels.add(tabs).removeClass(conf.classNames.active);
      labels.eq(tabId).add(tabs.eq(tabId)).addClass(conf.classNames.active);
      return onChange();
    };
    getCurrent = function() {
      if (labels.filter(conf.selectors.active).length !== 0) {
        return openTab(labels.filter(conf.selectors.active).index());
      } else {
        return openTab(0);
      }
    };
    onLabelClick = function(evt) {
      openTab($(this).index());
      return evt.preventDefault();
    };
    bindLabelClick = function() {
      return labels.on('click', onLabelClick);
    };
    init = function() {
      getCurrent();
      return bindLabelClick();
    };
    init();
    return self;
  }, ['ui', 'tabs', 'create'], false);

}).call(this);
