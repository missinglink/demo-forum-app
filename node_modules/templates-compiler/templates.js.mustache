(function() {
    var template, id, templates = {
    {{#templates}}
        "{{ns}}": {{{template}}}{{^last}},{{/last}}
    {{/templates}}
    };

    for (id in templates) {
        template = templates[id];
        template = new Hogan.Template(template);
        app.template.set(id, template);
    }

})();